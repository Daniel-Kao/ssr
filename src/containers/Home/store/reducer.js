const defaultState = {
  newsList: [],
  name: 'daniel',
};

const reducer = (state = defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
